<Window x:Class="Wpf___PC_Koncorocny_projekt.SearchDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Detail" Height="800" Width="1200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#0B000000" Height="48">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnClose" Content="✖" Width="48" Height="32" Margin="8" Click="BtnClose_Click"/>
            </StackPanel>
        </Border>

        <!-- WebView2 (Edge) control replaces legacy WebBrowser -->
        <wv2:WebView2 x:Name="Browser" Grid.Row="1" />
    </Grid>
</Window>
